const fucks = 0;
export {};
// import { RenderTarget, Texture } from "./graphics";
// import { Dims } from "./types";

// interface RenderPassTextureConfig {}

// interface RenderPassConfig {
//   textures: RenderPassTextureConfig[];
// }

// export class RenderPass extends RenderTarget {
//   private frameBuffer: WebGLFramebuffer;

//   private inputTextures: Texture[] = [];
//   private outputTextures: Texture[] = [];

//   constructor(private gl: WebGL2RenderingContext, private dims: Dims) {
//     super();
//     const fb = gl.createFramebuffer();
//     if (!fb) throw new Error("failed to create frameBuffer");
//     this.frameBuffer = fb;
//   }
// }
